# This directory is populated at release time by build_daemon.ps1 with the
# PyInstaller-frozen daemon. The placeholder keeps the Tauri resource glob
# valid for local builds; the launcher falls back to an external daemon when
# blankslate.exe is absent.
